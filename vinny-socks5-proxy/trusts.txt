# Файл настроек доверия перезагружается без перезапуска сервера - достаточно лишь сохранить файл
:new:root

# Уберите это, чтобы файл начал работать как блокировщик по белому списку
:command:accept:0

:call:allowed domains

:command:accept:0

:end:root


# Здесь перечисляются разрешённые домены второго уровня (их поддомены тоже разрешены)
# Для справки смотрите файлы example*.trusts
:new:allowed domains

:must:exactly:d[:1]
yandex.ru

# Для отмены forward-директивы, если такая есть в conf-файле
# :forward:socks5:none:

# Поток будет спать какое-то время перед тем, как возобновить передачу, если скорость передачи на соединение превысит 32000
# Обратите внимание, если у сервера несколько соединений, каждое разграничивается отдельно (например, 3 соединения дадут общую скорость 32*3=96 кб)
# При изменении настройки sleep, новые соединения получат новую настройку, но старые соединения будут работать по старой настройке
# :sleep:32000

:end:allowed domains
